{
  "name": "vue-libs",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean-install": "rm -rf **node_modules ** rm -rf bun.lockb && bun install"
  },
  "devDependencies": {
    "eslint-config-vue": "workspaces:*"
  }
}